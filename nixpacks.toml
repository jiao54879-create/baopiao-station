# Railway Nixpacks 配置
# 指定构建 backend 目录

[phases.build]
cmds = [
  "cd backend && npm install",
  "cd backend && npx prisma generate"
]

[phases.setup]
nixPkgs = ["nodejs_20"]

[start]
cmd = "cd backend && npm start"
